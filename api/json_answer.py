# generating json for answer


def generated_json(action):
    action_play = '{"action": "' + action + '"}'
    return action_play


def calculated_json(action):
    action_play = '{"action": "' + action + '"}'
    return action_play
